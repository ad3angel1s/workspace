env = Environment() # Initialize the environment

SConscript("monitor/SConscript", variant_dir="build/monitor", duplicate=0)
SConscript("google/SConscript", variant_dir="build/google", duplicate=0)
SConscript("longest-common-subsequence/SConscript", variant_dir="build/longest-common-subsequence", duplicate=0)
SConscript("sorting/SConscript", variant_dir="build/sorting", duplicate=0)
